spring:
  datasource:
    url: jdbc:mysql://localhost:3306/community_groupon?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    password:
    database: 1
  security:
    user:
      name: admin
      password: admin

server:
  port: 8080

jwt:
  secret: community_groupon_secret_key_for_jwt_token_generation_2024_secure
  expiration: 86400000

ai:
  dashscope:
    api-key: ${ALAPI_KEY}
  model: qwen-plus
  base-url: https://dashscope.aliyuncs.com/compatible-mode/v1